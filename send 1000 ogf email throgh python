import smtplib as s

ob = s.SMTP('smtp.gmail.com',587)
ob.ehlo()
ob.starttls()
ob.login('mail address','pass')
subject="test python"
body="i love python"
massage="subject:{}\n\n{}".format(subject,body)
listadd=[all mail address]
ob.sendmail(mail address)
print("send a mail")
ob.quit()
